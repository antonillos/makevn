#!/usr/bin/env bash
set -euo pipefail

repo="${MAKEVN_REPO:-antonillos/makevn}"

git ls-remote --tags --refs "https://github.com/${repo}.git" \
  | sed -nE 's#.*refs/tags/v?([0-9]+\.[0-9]+\.[0-9]+(-[A-Za-z0-9.]+)?)$#\1#p' \
  | sort -V
